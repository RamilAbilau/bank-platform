export interface LoginFormData {
    email: string;
    password: string;
}

export interface RegisterFormData {
    name: string;
    surname: string;
    passportId: string;
    email: string;
    password: string;
    confirmPassword: string;
}